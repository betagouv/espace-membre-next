import { test, expect } from "@playwright/test";

// test.use({ storageState: "./playwright-admin-auth.json" });

// test("account page", async ({ page }) => {
//     // page is authenticated as admin
//     await expect(page.getByText("Mon Compte").first()).toBeVisible();
// });
